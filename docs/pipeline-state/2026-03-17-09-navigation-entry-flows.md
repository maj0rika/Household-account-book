## Pipeline State

### 요청 요약
- 사이드 네비게이션 각 메뉴의 최초 진입 흐름을 Mermaid로 문서화한다;
- 대상은 거래 내역, 통계, 직접 입력, 자산/부채, 예산, 설정이다;
- 기존 상세 플로우는 유지하고, 초기 접근 흐름만 상단에 보강한다;

### 수용 기준
- [x] 각 사이드 메뉴별 최초 진입 흐름이 Mermaid로 추가된다;
- [x] 라우트, 초기 데이터 로드, 기본 상태가 코드 기준으로 반영된다;
- [x] 기존 상세 플로우와 중복되지 않게 초기 접근 흐름만 짧게 정리된다;
- [x] `docs/flowchart/*.md`와 `docs/flowchart/pdf/*.pdf`가 갱신된다;
- [x] `docs/history/`와 `docs/implementation-plan.md`가 갱신된다;

### 활성 Phase
- PM;

### 스킵 Phase
- UXUI: 앱 UI 변경이 아니라 문서 보강 작업이다;
- BE: 서버 로직 변경이 없다;
- FE: 화면 구현 변경이 없다;
- Infra: 배포/환경 변경이 없다;
- QA: 문서 전용 작업으로 별도 QA Phase를 실행하지 않는다;

### 승인 이력
- PM 계획 / 승인됨 / 네비게이션 최초 진입 흐름 추가, PDF 재생성, 기록 반영;

### 변경 패킷
- `00-navigation-overview.md`에 메뉴별 최초 진입 요약 차트 2개를 추가했다;
- `02`, `03`, `04`, `05`, `06` 문서 상단에 각 메뉴의 초기 접근 차트를 추가했다;
- 갱신된 Markdown 기준으로 PDF를 다시 생성했고, 대표 파일의 페이지 수가 1페이지로 유지되는 것을 확인했다;
- `docs/history/2026-03-17-09-navigation-entry-flows.md`를 추가하고 `docs/implementation-plan.md`를 갱신했다;

### 리뷰 이슈
- 없음;

### QA 상태
- SKIPPED;
- 재시작 지점: 없음;

### PM 최종 판정
- PASS;
- 근거: 메뉴별 최초 진입 흐름이 문서에 추가됐고, PDF 재생성 및 기록 반영까지 완료했다;

### 다음 액션
- 사용자 확인이 필요하다;
